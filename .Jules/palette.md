## 2024-05-23 - Accessibility in Developer Tools
**Learning:** Developer-focused tools often overlook accessibility in critical navigation elements (like file tabs), assuming "power users" don't need semantic labels.
**Action:** Always verify that icon-only buttons (especially destructive ones like "Close") have `aria-label` and `title` attributes, even in "minimalist" interfaces.
